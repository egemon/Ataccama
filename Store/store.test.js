import store from '../Store/store';
import assert from 'assert';
describe('Store', () => {
	describe('router', () => {
		it('should add ', () => {

			assert.deepEqual(store.getState(), state);
		});
	});
});